; The app's main config file. genai/sql-mapper's DatabaseProperty binds the
; [database] group below (no separate database file). Swap the dsn for a real
; mysql:/pgsql: connection in production.

[database]
dsn      = "sqlite::memory:"
username =
password =
